<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 为这个mapper指定一个唯一的namespace，namespace的值习惯上设置成包名+sql映射文件名，这样就能够保证namespace的值是唯一的 
	例如namespace="me.gacl.mapping.userMapper"就是me.gacl.mapping(包名)+userMapper(userMapper.xml文件去除后缀) -->
<mapper namespace="com.equipments.management.mapper.DocsProductMapper">

	<select id="getDocsProductByProductNo" parameterType="Integer"
		resultType="java.util.Map">
		SELECT
			DOCS_PRODUCT_ID docsproductid,
			PRODUCT_NO productno,
			DOCS_FILENAME docsfilename,
			SELECTED selected
		FROM
			eq_docs_product
		WHERE
			PRODUCT_NO = #{productno}
	</select>
	
	<update id="updateDocsProduct" parameterType="java.util.Map">
        update eq_docs_product set SELECTED=#{selected}
        where DOCS_PRODUCT_ID=#{docsproductid}
    </update>
	
</mapper>
